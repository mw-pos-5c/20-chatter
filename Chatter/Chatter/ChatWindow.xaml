<Window x:Class="Chatter.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chatter"
        mc:Ignorable="d"
        Title="ChatWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50px"/>
            <RowDefinition />
            <RowDefinition Height="50px"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding Path=Title}" HorizontalContentAlignment="Center" />

        <ListView Grid.Row="1" ItemsSource="{Binding Path=Messages}" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Msg:"
                   VerticalAlignment="Center" HorizontalAlignment="Center" />
            <TextBox Grid.Column="1"></TextBox>
            <Button Grid.Column="2">Send</Button>
        </Grid>
    </Grid>
</Window>
